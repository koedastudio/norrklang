{
  "name": "norrklang-www",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "check": "astro check",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.3"
  },
  "//overrides": "Works around transitive pins from miniflare (via wrangler) — see README §Notes.",
  "overrides": {
    "sharp": "^0.35.3",
    "undici": "^7.29.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "sharp": "^0.35.3",
    "typescript": "^6.0.3",
    "wrangler": "^4.112.0"
  }
}
